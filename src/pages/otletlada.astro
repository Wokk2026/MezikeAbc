---
// Mézike ABC - Ötletláda (Interaktív animációkkal)
const boltNev = "Mézike ABC";
const cim = "1238 Budapest, Grassalkovich út (A nagy templom mögött)";
---

<html lang="hu">
	<head>
		<meta charset="utf-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>Ötletláda - {boltNev}</title>
		<style>
			:root {
				--ginger-brown: #d2691e;
				--shrek-green: #7dbb42;
				--warm-cream: #fff9f0;
				--deep-brown: #5d3a1a;
                --botanical-bg: #f0f7e9;
			}

            * { -webkit-tap-highlight-color: transparent; outline: none; box-sizing: border-box; }

			body {
				font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
				background: var(--warm-cream);
				color: var(--deep-brown);
				margin: 0;
				overflow-x: hidden;
			}

			.container { max-width: 800px; margin: 0 auto; padding: 0 20px; }
			
			header { 
				background: rgba(255, 255, 255, 0.95); 
				padding: 1rem 0; 
				border-bottom: 4px solid var(--shrek-green);
				position: sticky; top: 0; z-index: 1000;
			}
			.nav-flex { display: flex; justify-content: space-between; align-items: center; }
            .logo { font-size: 1.5rem; font-weight: bold; color: var(--ginger-brown); text-decoration: none; }
            nav a { margin: 0 10px; text-decoration: none; color: var(--deep-brown); font-weight: 600; }

            .main-content { padding: 4rem 0; text-align: center; min-height: 80vh; }

            /* 1. Lépés: Választó gombok */
            .choice-container { display: flex; justify-content: center; gap: 30px; margin-bottom: 3rem; }
            .choice-card {
                background: white;
                padding: 2rem;
                border-radius: 30px;
                border: 3px solid var(--ginger-brown);
                cursor: pointer;
                transition: 0.3s;
                width: 150px;
            }
            .choice-card:hover { transform: translateY(-10px); border-color: var(--shrek-green); }
            .choice-card.active { background: var(--botanical-bg); border-color: var(--shrek-green); }
            
            .icon { font-size: 3rem; display: block; margin-bottom: 10px; transition: 0.5s; }

            /* Animációk: Villanykörte és Csillag */
            @keyframes bulbFlash {
                0% { filter: drop-shadow(0 0 0px yellow); transform: scale(1); }
                50% { filter: drop-shadow(0 0 30px yellow); transform: scale(1.2); }
                100% { filter: drop-shadow(0 0 10px yellow); transform: scale(1.1); }
            }
            .flash { animation: bulbFlash 0.6s ease-in-out infinite alternate; }

            @keyframes starGlow {
                0% { filter: drop-shadow(0 0 0px #7dbb42); transform: rotate(0deg); }
                100% { filter: drop-shadow(0 0 25px #7dbb42); transform: rotate(15deg) scale(1.2); }
            }
            .glow { animation: starGlow 0.8s ease-in-out infinite alternate; }

            /* 2. Lépés: Szövegdoboz */
            #form-section { display: none; opacity: 0; transform: translateY(20px); transition: 0.5s; }
            #form-section.visible { display: block; opacity: 1; transform: translateY(0); }

            textarea {
                width: 100%;
                max-width: 500px;
                height: 150px;
                border-radius: 20px;
                border: 3px solid var(--ginger-brown);
                padding: 1.5rem;
                font-family: inherit;
                font-size: 1rem;
                margin-bottom: 1.5rem;
                resize: none;
            }

            .send-btn {
                background: var(--ginger-brown);
                color: white;
                border: none;
                padding: 15px 40px;
                border-radius: 50px;
                font-size: 1.2rem;
                font-weight: bold;
                cursor: pointer;
                transition: 0.3s;
            }
            .send-btn:hover { background: var(--deep-brown); transform: scale(1.05); }

            /* 3. Lépés: Postaláda és Levél Animáció */
            #mailbox-container {
                position: fixed;
                bottom: -300px;
                left: 50%;
                transform: translateX(-50%);
                transition: 0.8s cubic-bezier(0.175, 0.885, 0.32, 1.275);
                z-index: 2000;
                text-align: center;
            }
            #mailbox-container.up { bottom: 50px; }
            
            .mailbox { font-size: 8rem; position: relative; }
            .mailbox-lid { position: absolute; top: 20px; left: 20px; transition: 0.5s; transform-origin: top; }
            .mailbox.open .mailbox-lid { transform: rotateX(-110deg); }

            #envelope-animation {
                position: fixed;
                width: 60px;
                height: 40px;
                background: white;
                border: 2px solid var(--deep-brown);
                display: none;
                z-index: 2100;
                border-radius: 5px;
            }

            /* Sikeres üzenet */
            #success-msg { display: none; margin-top: 2rem; font-size: 1.5rem; font-weight: bold; color: var(--shrek-green); }

            @media (max-width: 600px) {
                .choice-container { gap: 15px; }
                .choice-card { width: 130px; padding: 1rem; }
            }

			footer { background: var(--deep-brown); color: white; padding: 3rem 0; text-align: center; margin-top: 4rem; }
		</style>
	</head>
	<body>
		<header>
			<div class="container nav-flex">
				<a href="/" class="logo">🍪 {boltNev}</a>
                <nav>
                    <a href="/">Főoldal</a>
                    <a href="/akciok">Akciók</a>
                    <a href="/jatek">Kvíz</a>
                    <a href="/otletlada">Ötletláda</a>
                </nav>
			</div>
		</header>

		<main class="container main-content">
            <h1 id="title-text">Mocsári Ötletláda</h1>
            <p id="sub-text">Oszd meg velünk gondolataidat névtelenül!</p>

            <div class="choice-container" id="step-1">
                <div class="choice-card" onclick="selectType('otlet', this)">
                    <span class="icon" id="bulb-icon">💡</span>
                    <strong>Ötlet</strong>
                </div>
                <div class="choice-card" onclick="selectType('velemeny', this)">
                    <span class="icon" id="star-icon">⭐</span>
                    <strong>Vélemény</strong>
                </div>
            </div>

            <div id="form-section">
                <textarea id="message" placeholder="Írd ide a mocsári üzeneted..."></textarea><br>
                <button class="send-btn" onclick="sendMessage()">Küldés</button>
            </div>

            <div id="success-msg">Köszönjük a véleményt, a mocsár nevében! 💚</div>
		</main>

        <div id="mailbox-container">
            <div class="mailbox" id="mailbox-body">📮</div>
            <div id="envelope-animation"></div>
        </div>

		<footer>
			<div class="container">
				<p>📍 {cim}</p>
				<p>© 2026 {boltNev}</p>
			</div>
		</footer>

        <script>
            let selectedType = "";

            function selectType(type, element) {
                selectedType = type;
                
                // Gombok resetelése
                document.querySelectorAll('.choice-card').forEach(c => c.classList.remove('active'));
                document.getElementById('bulb-icon').classList.remove('flash');
                document.getElementById('star-icon').classList.remove('glow');
                
                // Aktiválás és animáció
                element.classList.add('active');
                if(type === 'otlet') document.getElementById('bulb-icon').classList.add('flash');
                else document.getElementById('star-icon').classList.add('glow');

                // Form megjelenítése
                document.getElementById('form-section').classList.add('visible');
            }

            function sendMessage() {
                const text = document.getElementById('message').value;
                if(!text) return alert("Kérlek, írj valamit a papírra!");

                const btn = document.querySelector('.send-btn');
                const form = document.getElementById('form-section');
                const mailboxContainer = document.getElementById('mailbox-container');
                const envelope = document.getElementById('envelope-animation');

                btn.style.display = 'none';

                // 1. Postaláda beúszik
                mailboxContainer.classList.add('up');

                setTimeout(() => {
                    // 2. Levél animáció indítása
                    const rect = document.getElementById('message').getBoundingClientRect();
                    envelope.style.display = 'block';
                    envelope.style.left = rect.left + (rect.width / 2) - 30 + 'px';
                    envelope.style.top = rect.top + 'px';
                    envelope.style.opacity = '1';

                    // 3. Papír borítékká hajlik és berepül
                    setTimeout(() => {
                        envelope.style.transition = "1s ease-in-out";
                        envelope.style.transform = "scale(0.5) rotate(720deg)";
                        envelope.style.left = "50%";
                        envelope.style.top = (window.innerHeight - 150) + "px";
                        envelope.style.opacity = "0";
                    }, 100);

                    // 4. Befejezés
                    setTimeout(() => {
                        form.style.display = 'none';
                        document.getElementById('step-1').style.display = 'none';
                        document.getElementById('title-text').style.display = 'none';
                        document.getElementById('sub-text').style.display = 'none';
                        document.getElementById('success-msg').style.display = 'block';
                        
                        // Postaláda visszaugrik
                        setTimeout(() => mailboxContainer.classList.remove('up'), 2000);
                    }, 1100);
                }, 800);
            }
        </script>
	</body>
</html>