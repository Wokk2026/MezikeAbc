---
// Mézike ABC - Akciós oldal (Mobil-optimalizált verzió)
const boltNev = "Mézike ABC";
const telefon = "+3612853245"; 
const cim = "1238 Budapest, Grassalkovich út (A nagy templom mögött)";
---

<html lang="hu">
	<head>
		<meta charset="utf-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>Akciók - {boltNev}</title>
		<style>
			:root {
				--ginger-brown: #d2691e;
				--shrek-green: #7dbb42;
				--warm-cream: #fff9f0;
				--deep-brown: #5d3a1a;
                --glow-green: rgba(125, 187, 66, 0.4);
			}

/* --- GLOBÁLIS SIMÍTÁSOK (Kék keret ellen) --- */
* {
    /* Ez a kulcs! Eltünteti a kék villanást mobilon */
    -webkit-tap-highlight-color: transparent;
    /* Ez pedig a fókusz keretet tünteti el, ha zavaró */
    outline: none;
}

/* Biztos ami biztos alapon a karakterekre külön is rátesszük */
#szamar-wrapper, #mezike-wrapper, #shrek-footer-wrapper, #fiona-wrapper, a, button {
    -webkit-tap-highlight-color: transparent;
    outline: none;
}

			html { scroll-behavior: smooth; }

			body {
				font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
				background: var(--warm-cream);
				color: var(--deep-brown);
				margin: 0;
				line-height: 1.6;
                overflow-x: hidden;
			}

			.container { max-width: 900px; margin: 0 auto; padding: 0 20px; position: relative; z-index: 10; }
			
			/* Navigáció */
			header { 
				background: rgba(255, 255, 255, 0.95); 
				backdrop-filter: blur(5px);
				padding: 1rem 0; 
				border-bottom: 4px solid var(--shrek-green);
				position: sticky; top: 0; z-index: 1000;
			}
			.nav-flex { display: flex; justify-content: space-between; align-items: center; }
			.logo { font-size: 1.5rem; font-weight: bold; color: var(--ginger-brown); text-decoration: none; }
            nav a { margin: 0 10px; text-decoration: none; color: var(--deep-brown); font-weight: 600; transition: 0.3s; }
            nav a:hover { color: var(--shrek-green); }

			/* Hero szekció */
			.hero-akciok { 
				padding: 5rem 0; 
				text-align: center; 
				background: linear-gradient(rgba(93, 58, 26, 0.7), rgba(93, 58, 26, 0.7)), url('https://images.unsplash.com/photo-1512484491140-5a7c2957975c?auto=format&fit=crop&q=80&w=1200');
				background-size: cover;
				background-position: center;
				border-radius: 0 0 50px 50px;
                color: white;
                border-bottom: 6px solid var(--ginger-brown);
			}

			/* Akciós kártyák */
			.akcio-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); gap: 30px; margin: 4rem 0; }
			.akcio-card { 
				background: white; 
				padding: 0; 
				border-radius: 30px; 
				overflow: hidden;
				box-shadow: 0 10px 30px rgba(0,0,0,0.1);
				border: 3px solid var(--ginger-brown);
                transition: 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
                position: relative;
                z-index: 20;
			}
            .akcio-card:hover { transform: translateY(-10px); border-color: var(--shrek-green); box-shadow: 0 15px 35px var(--glow-green); }
            .akcio-tag { background: var(--shrek-green); color: white; padding: 10px; text-align: center; font-weight: bold; letter-spacing: 1px; }
            .akcio-info { padding: 2rem; text-align: center; }
            .price { font-size: 2.5rem; color: var(--ginger-brown); font-weight: bold; display: block; margin: 10px 0; }

            /* INTERAKTÍV FIONA (Mobilbarát verzió) */
            #fiona-wrapper {
                position: fixed;
                left: -300px; 
                bottom: 40px;
                z-index: 1500;
                transition: left 0.6s cubic-bezier(0.175, 0.885, 0.32, 1.275);
                display: flex;
                flex-direction: column;
                align-items: flex-start;
                pointer-events: none;
            }
            #fiona-wrapper.show { left: 20px; pointer-events: auto; }
            
            #fiona-img { 
                width: 180px; 
                filter: drop-shadow(5px 5px 15px rgba(0,0,0,0.2)); 
            }

            #fiona-bubble {
                background: white;
                padding: 15px 25px;
                border-radius: 25px 25px 25px 0;
                box-shadow: 0 5px 20px rgba(0,0,0,0.15);
                border: 3px solid var(--shrek-green);
                font-weight: bold;
                margin-bottom: 15px;
                max-width: 280px;
                color: var(--deep-brown);
                opacity: 0;
                transition: 0.3s;
            }
            #fiona-wrapper.show #fiona-bubble { opacity: 1; }

            /* HULLÓ SÜTIK */
            #cookie-shower {
                position: fixed; top: 0; left: 0; width: 100%; height: 100%;
                pointer-events: none; z-index: 5;
            }
            .falling-cookie {
                position: absolute; font-size: 4rem; user-select: none;
                filter: drop-shadow(0 5px 8px rgba(0,0,0,0.15));
            }

            /* --- MOBIL SZABÁLYOK --- */
            @media (max-width: 768px) {
                .hero-akciok h1 { font-size: 2.2rem; }
                .nav-flex { flex-direction: column; gap: 10px; }
                
                #fiona-img { width: 120px; }
                #fiona-bubble { font-size: 0.8rem; max-width: 180px; padding: 10px 15px; }
                
                .akcio-grid { gap: 20px; margin: 2rem 0; }
                .akcio-info { padding: 1.5rem; }
                .price { font-size: 2rem; }
                
                footer { padding: 3rem 0 6rem 0; }
            }

			footer { background: var(--deep-brown); color: white; padding: 4rem 0; text-align: center; }
		</style>
	</head>
	<body>
        <div id="cookie-shower"></div>

        <div id="fiona-wrapper">
            <div id="fiona-bubble">Shrek szerint ezek az árak még nála is zöldebbek! 💚</div>
            <img src="/fiona.png" id="fiona-img" alt="Fiona hercegnő" />
        </div>

		<header>
			<div class="container nav-flex">
				<a href="/" class="logo">{boltNev}</a>
                <nav>
    <a href="/">Főoldal</a>
<a href="/otletlada">Ötletláda</a>
    <a href="/akciok">Akciók</a>
    <a href="/jatek">Kvíz</a> </nav>
				<a href="/#kapcsolat" class="btn" style="background: var(--ginger-brown); color: white; padding: 10px 20px; border-radius: 50px; text-decoration: none; font-weight: bold; font-size: 0.9rem;">Kapcsolat</a>
			</div>
		</header>

		<main>
			<section class="hero-akciok">
				<div class="container">
					<h1>Nagylepény Akciós Kunyhója</h1>
					<p>Süteményes eső és mocsári árak várnak!</p>
				</div>
			</section>

			<div class="container">
				<div class="akcio-grid">
					<div class="akcio-card">
						<div class="akcio-tag">MÉZIKÉS AJÁNLAT</div>
						<div class="akcio-info">
							<h3>Dupla Csokis Cookie</h3>
							<span class="price">390 Ft</span>
							<p>Vigyázz, Mézike ne lássa meg, hogy megeszed a barátait!</p>
						</div>
					</div>

					<div class="akcio-card">
						<div class="akcio-tag">FIONA KEDVENCE</div>
						<div class="akcio-info">
							<h3>Mocsári Krémes</h3>
							<span class="price">550 Ft</span>
							<p>Zöld, édes, és nincs benne hagyma!</p>
						</div>
					</div>

                    <div class="akcio-card">
						<div class="akcio-tag">NAGYLEPÉNY RECEPTJE</div>
						<div class="akcio-info">
							<h3>Óriás Perec</h3>
							<span class="price">490 Ft</span>
							<p>Olyan ropogós, hogy Sárkány is ezt eszi.</p>
						</div>
					</div>
				</div>
			</div>
		</main>

		<footer>
			<div class="container">
				<p>📍 {cim}</p>
				<p>© 2026 {boltNev}</p>
			</div>
		</footer>

        <script>
            // SÜTI-ESŐ
            const shower = document.getElementById('cookie-shower');
            const cookies = ['🍪', '🧁', '🥯', '🥨', '🍩'];
            
            function dropCookie() {
                const cookie = document.createElement('div');
                cookie.className = 'falling-cookie';
                cookie.innerText = cookies[Math.floor(Math.random() * cookies.length)];
                
                const isLeft = Math.random() > 0.5;
                const posX = isLeft ? (Math.random() * 15) : (85 + Math.random() * 10);
                
                cookie.style.left = posX + '%';
                cookie.style.top = '-100px';
                
                const speed = 2 + Math.random() * 4;
                let posY = -100;
                let rotation = 0;

                shower.appendChild(cookie);

                function animate() {
                    posY += speed;
                    rotation += 1;
                    cookie.style.top = posY + 'px';
                    cookie.style.transform = `rotate(${rotation}deg)`;

                    if (posY < window.innerHeight + 100) {
                        requestAnimationFrame(animate);
                    } else {
                        cookie.remove();
                    }
                }
                requestAnimationFrame(animate);
            }
            setInterval(dropCookie, 700);

            // FIONA INTERAKCIÓ (Mobil + Desktop)
            const cards = document.querySelectorAll('.akcio-card');
            const fiona = document.getElementById('fiona-wrapper');
            const bubble = document.getElementById('fiona-bubble');
            
            const fionaMsgs = [
                "Shrek szerint ezek az árak perzselően jók! ✨",
                "Ezek az árak szebbek, mint egy mocsári naplemente. 🌅",
                "Végre valami, ami nem hagyma! 🧅",
                "Siess, mert Szamár már majdnem mindet megette! 🐴"
            ];

            const triggerFiona = () => {
                bubble.innerText = fionaMsgs[Math.floor(Math.random() * fionaMsgs.length)];
                fiona.classList.add('show');
                setTimeout(() => fiona.classList.remove('show'), 4000);
            };

            // Desktopon hover, mobilon érintés a kártyákra
            cards.forEach(card => {
                card.addEventListener('mouseenter', triggerFiona);
                card.addEventListener('touchstart', (e) => { triggerFiona(); });
            });
        </script>
	</body>
</html>