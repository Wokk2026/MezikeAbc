---
const { esemeny, full = false } = Astro.props;
---
<div class="card" style={`border-top: 5px solid ${esemeny.id % 2 === 0 ? '#7dbb42' : '#d2691e'}; margin-bottom: 20px;`}>
    <div style="font-weight: bold; color: #7dbb42; font-size: 0.9rem; margin-bottom: 5px;">
        📅 {esemeny.datum}
    </div>
    <h3 style="margin: 0 0 10px 0;">{esemeny.cim}</h3>
    <p style="font-size: 0.95rem; color: #5d3a1a;">
        {full ? esemeny.reszletes : esemeny.rovid}
    </p>

    {!full && <a href="/esemenyek" style="color: #d2691e; text-decoration: none; font-weight: bold; font-size: 0.8rem;">Részletek →</a>}
</div>